server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
#ribbon:
#  #指的是建立连接后从服务器读取到可用资源所用的时间
#
#  ReadTimeout: 5000
#  #指的是建立连接所用时间，适用于网络状况的情况下，两端连接所用的时间

#  ConnectTimeOut: 5000
#开启hystrix
feign:
  hystrix:
    enabled: true