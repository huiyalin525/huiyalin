server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureka实例的主机名
  client:
    register-with-eureka: false #它的作用是不把eureka本身在注册在eureka注册中心中
    fetch-registry: false #不从eureka上来获取服务的注册信息
    service-url:
      {defaultzone: 'http://localhost:8761/eureka/'}
